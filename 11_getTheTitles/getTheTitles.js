const getTheTitles = function (arr) {
  return arr.map((book) => book.title);
};
// make names array
// for each obj in array
// get obj.title (array[0].title?)
// push to names array

// Do not edit below this line
module.exports = getTheTitles;
